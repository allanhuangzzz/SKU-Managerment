# 产品SKU管理系统 — 开发文档

> 面向下次开发的交接文档。记录项目结构、数据模型、接口清单、核心计算逻辑、前端模块与近期改动，便于快速接手。
>
> 最后更新：2026-08-31

---

## 一、项目概述

一个用于跨境电商选品/定价的本地 Web 工具。核心能力：

- **产品信息**：录入 SKU、尺寸、进货价、国内运费、重量、货物类型、图片。
- **运费维护**：按国家维护多条运费规则（渠道 × 货物类型 × 重量区间，含首费 `per_parcel` + 续费 `per_kg`）。
- **佣金维护**：按国家维护平台佣金率（默认德/法/意/西 15%，其余 13%）。
- **汇率维护**：手动维护 + 公开接口自动刷新（外币兑人民币）。
- **产品运费明细（核心）**：展开产品行后，按其重量与货物类型匹配各国运费规则，实时计算**盈亏平衡售价**、**预估收益**、**平台佣金**等，并支持行内录入售价。

### 技术栈

| 层 | 技术 |
| --- | --- |
| 后端 | Python + Flask（`flask>=3.0`），单文件路由 `app.py` |
| 数据库 | SQLite（`sku.db`），schema 在 `db.py` |
| 前端 | 原生 HTML/CSS/JS（无框架、无构建），按模块拆分 JS |
| 汇率 | open.er-api.com（主）/ frankfurter.app（备） |

---

## 二、目录结构

```
产品SKU记录/
├── app.py                # Flask 后端：全部 API 路由 + 汇率抓取 + 计算逻辑
├── db.py                 # SQLite 连接、schema、种子数据、迁移
├── launcher.py           # 一键启动器：建 venv → 装依赖 → 启动 → 打开网页
├── import_shipping.py    # 运费模板批量导入脚本（幂等，覆盖涉及国家）
├── requirements.txt      # 依赖：flask>=3.0
├── sku.db                # SQLite 数据库文件（运行时生成/持有数据）
├── 启动系统.bat           # Windows 双击入口：定位 Python 并调用 launcher.py
├── .venv/                # 启动器自动创建的虚拟环境（首次运行生成，勿提交）
├── templates/
│   └── index.html        # 单页骨架：侧边栏 + 4 个视图 + 弹窗/toast 容器
└── static/
    ├── css/style.css     # 全部样式
    ├── js/
    │   ├── app.js        # 公共层：api()、toast、openModal、格式化、视图切换
    │   ├── products.js   # 产品信息模块（含运费明细表 + 售价录入）
    │   ├── shipping.js   # 运费维护模块
    │   ├── commissions.js# 佣金维护模块
    │   └── rates.js      # 汇率维护模块
    └── uploads/          # 产品图片上传目录
```

---

## 三、数据模型（SQLite）

schema 定义见 [db.py](file:///c:/Users/80550/Desktop/AI项目/产品SKU记录/db.py#L10-L70)。

| 表 | 关键字段 | 说明 |
| --- | --- | --- |
| `countries` | `name` UNIQUE, `code`, `currency_code`, `sort_order` | 国家 + 币种 + 排序。种子 28 国。 |
| `shipping_rules` | `country_id`, `channel`, `cargo_type`, `currency`, `weight_min`, `weight_max`(可空=无上限), `per_parcel`, `per_kg`, `remark` | 运费规则；一条规则=某重量区间的首费+续费。 |
| `products` | `sku` UNIQUE, `name`, `category`, `cargo_type`(默认 普货), `length/width/height`, `purchase_price`, `domestic_shipping`, `weight`, `image_path`, `remark` | 产品主表。 |
| `exchange_rates` | `currency_code` UNIQUE, `rate_to_cny`, `source`(auto/manual), `updated_at` | 1 外币 = ? 人民币。 |
| `commission_rates` | `country_id` UNIQUE, `rate` | 平台佣金率（百分数，如 13.0）。 |
| `product_selling_prices` | `product_id`+`country_id`+`channel` UNIQUE, `selling_price` | 产品在某国某渠道录入的售价。 |

**货物类型**：`普货`、`带电`、`敏感货`、`特货`（前端 `Products.cargoTypes`）。

**迁移逻辑**：`init_db()` 用 `ALTER TABLE ... ADD COLUMN` 幂等补列（`cargo_type/length/width/height/image_path`），旧库可平滑升级。见 [db.py](file:///c:/Users/80550/Desktop/AI项目/产品SKU记录/db.py#L119-L143)。

---

## 四、核心计算逻辑（务必理解）

位于 `GET /api/products/<pid>/shipping` — [app.py](file:///c:/Users/80550/Desktop/AI项目/产品SKU记录/app.py#L266-L351)。

1. **规则匹配**：取产品 `weight` 与 `cargo_type`，筛选 `weight_min ≤ weight ≤ weight_max`（`weight_max` 为空表示无上限）的规则。
   - ⚠️ **已知限制**：若产品重量超过所有区间上限，会匹配不到任何规则（明细为空）。是否把最大区间上限改为 `NULL`（无限）需业务确认。
2. **原始运费**：`cost_original = per_parcel + per_kg × weight`（当地币种）。
3. **人民币运费**：`cost_cny = cost_original × rate`（`rate` = 该币种兑人民币）。
4. **盈亏平衡售价（人民币）**：
   `break_even = (purchase_price + domestic_shipping + agent_fee + cost_cny) / (1 - 佣金率)`
   （`agent_fee` = 货代处理费，2026-09 新增字段，计入产品成本）
5. **盈亏平衡售价（当地币种）**：`break_even_local = break_even / rate`。
6. **平台佣金**：
   - 无录入售价时 = `break_even × 佣金率`；
   - 有录入售价时 = `selling_price × rate × 佣金率`（按实际售价重算）。
7. **预估收益**：
   - 当地币种：`estimated_profit = selling_price − break_even_local`；
   - 人民币：`estimated_profit_rmb = estimated_profit × rate`。

返回字段附带 `currency` 与 `currency_symbol`（符号映射见 [app.py](file:///c:/Users/80550/Desktop/AI项目/产品SKU记录/app.py#L27-L35)）。

---

## 五、API 清单

全部返回 JSON，`app.json.ensure_ascii = False`（中文不转义）。约定成功 `{"ok": true, ...}`，失败 `{"ok": false, "message": "..."}` + 对应 HTTP 状态码。

### 产品
| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/products` | 产品列表（按 `updated_at` 倒序） |
| POST | `/api/products` | 新增（`sku`/`name` 必填，`sku` 唯一） |
| PUT | `/api/products/<pid>` | 更新 |
| DELETE | `/api/products/<pid>` | 删除 |
| POST | `/api/products/<pid>/image` | 上传图片（multipart，扩展名 png/jpg/jpeg/gif/webp/bmp） |
| PUT | `/api/products/<pid>/selling-prices` | 批量写入/删除售价（body `{items:[{country_id,channel,selling_price}]}`，空值=删除） |
| GET | `/api/products/<pid>/shipping` | **各国运费明细 + 计算结果**（核心） |

### 国家 / 运费
| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/countries` | 国家列表 |
| POST | `/api/countries` | 新增国家（自动尝试拉取该币种汇率） |
| PUT | `/api/countries/<cid>` | 更新（币种变更时自动刷汇率） |
| DELETE | `/api/countries/<cid>` | 删除（币种无引用时联动删汇率） |
| PUT | `/api/countries/reorder` | 批量更新排序 |
| GET | `/api/shipping-rules?country_id=` | 运费规则列表 |
| POST/PUT/DELETE | `/api/shipping-rules[/<rid>]` | 单条运费规则增改删 |
| PUT | `/api/countries/<cid>/shipping-rules` | **整体替换**某国全部规则（共用币种） |
| DELETE | `/api/countries/<cid>/shipping-rules` | 清空某国全部规则 |

### 汇率 / 佣金
| 方法 | 路径 | 说明 |
| --- | --- | --- |
| GET | `/api/exchange-rates` | 各国币种汇率（LEFT JOIN，未设置为 null） |
| PUT | `/api/exchange-rates/<rid>` | 手动改汇率（source=manual） |
| POST | `/api/exchange-rates/refresh` | 从公开接口刷新全部币种 |
| GET | `/api/exchange-rates/status` | 是否过期（>6h 视为 stale） |
| GET | `/api/commissions` | 佣金列表 |
| PUT | `/api/commissions/<cid>` | 改佣金率（0~100，按 country_id） |

### 汇率自动刷新机制
- 启动 2 秒后后台静默刷新一次（仅当从未 auto 更新过），见 [app.py](file:///c:/Users/80550/Desktop/AI项目/产品SKU记录/app.py#L698-L707)。
- 前端进入产品页调用 `ensureFreshRates()`，`status` 报 stale 则静默刷新，见 [app.js](file:///c:/Users/80550/Desktop/AI项目/产品SKU记录/static/js/app.js#L113-L121)。

---

## 六、前端结构

- **入口骨架** [index.html](file:///c:/Users/80550/Desktop/AI项目/产品SKU记录/templates/index.html)：左侧 4 个 nav（产品/运费/佣金/汇率），右侧 `#toolbar` + 4 个 `.view` 区块 + 弹窗 `#modal-overlay` + toast `#toast-root`。
- **公共层** [app.js](file:///c:/Users/80550/Desktop/AI项目/产品SKU记录/static/js/app.js)：
  - `api(url, method, body)` — 统一 fetch 封装，自动处理 JSON / FormData 与错误。
  - `toast()` / `openModal()` / `confirmDialog()` — 提示与弹窗。
  - `fmtMoney/fmtNum/fmtWeightRange/esc` — 格式化与转义。
  - `switchView()` — 视图切换 + 各模块 `renderToolbar()`/`load()` 调度。
- **各模块**均为全局对象（`Products`/`Shipping`/`Commissions`/`Rates`），约定含 `renderToolbar(toolbar)` 与 `load()`。脚本按顺序在 index.html 底部引入，无模块化打包。

---

## 七、近期改动（本轮）

针对产品运费明细表（`Products.shippingHtml`）的显示与售价录入做了调整：

1. **字段居中 + 货币对齐**
   - 表头/单元格统一加 `.ctr` 类居中，见 [style.css](file:///c:/Users/80550/Desktop/AI项目/产品SKU记录/static/css/style.css#L213-L215)。
   - 金额用 `money-cell` 结构：符号列固定宽 16px 居左（保证同列符号位置一致），数字块 `min-width:62px` 右对齐 + `tabular-nums`（保证按位数对齐）。见 [style.css](file:///c:/Users/80550/Desktop/AI项目/产品SKU记录/static/css/style.css#L216-L224) 与 [products.js](file:///c:/Users/80550/Desktop/AI项目/产品SKU记录/static/js/products.js#L152-L157)。
2. **售价直接输入、无上下调整、限四位**
   - 输入框改 `type="text" inputmode="decimal"`（天然无 spinner），并用 CSS 兼容处理彻底去箭头。见 [products.js](file:///c:/Users/80550/Desktop/AI项目/产品SKU记录/static/js/products.js#L173-L180) 与 [style.css](file:///c:/Users/80550/Desktop/AI项目/产品SKU记录/static/css/style.css#L253-L265)。
   - 输入过滤：整数最多 4 位、小数最多 2 位，正则 `/^(\d{0,4})(\.(\d{0,2})?)?/`。见 [products.js](file:///c:/Users/80550/Desktop/AI项目/产品SKU记录/static/js/products.js#L92-L101)。
   - 保存后回显用 `String(it.selling_price)`（不带千分位），避免逗号污染输入框。

3. **产品图片支持粘贴/拖拽上传**
   - 产品新增/编辑弹窗的图片区为 `.img-upload-zone`，支持三种入口：**点击选择**、**拖拽图片到区域**、**Ctrl+V 粘贴截图**（微信/QQ/系统截图工具复制的位图均可）。
   - 三种入口统一写入闭包变量 `pendingFile`，保存产品后由 `uploadImage(pid)` 复用既有 multipart 接口 `POST /api/products/<pid>/image` 上传。
   - 粘贴的截图无文件名，前端 `normalizeImageFile()` 按 mime 补 `paste.png/.jpg/...`；后端 [app.py](file:///c:/Users/80550/Desktop/AI项目/产品SKU记录/app.py#L228-L236) 另有"无后缀按 mimetype 推断"兜底。
   - paste 监听挂在 document 上（仅弹窗打开时生效，每次打开先移除旧监听防累积）；剪贴板无图片时不拦截，文字粘贴（SKU/备注）不受影响。

4. **运费明细：售价 ↔ 预估收益（RMB）双向联动**
   - 「预估收益（RMB）」由只读文本改为可编辑输入框（带 ¥ 符号，`data-field="profit"`），与「售价」（`data-field="price"`）实时双向联动；「预估收益（当地币种）」为只读、随两者实时变色刷新。
   - 联动口径（与后端一致，`rate`=1 外币兑人民币、`be`=盈亏平衡售价当地币）：
     - 输售价 → `收益RMB = (售价 − be) × rate`
     - 输收益RMB → `售价 = be + 收益RMB ÷ rate`（收益可为负=亏损，售价为负则拦截提示）
   - 两个字段都只持久化**售价（当地币种）**：编辑收益RMB 时先反算售价，再复用 `PUT /api/products/<id>/selling-prices` 保存；收益RMB 始终为派生值，**无需改库表**。
   - rate/be 通过 `data-rate`/`data-be`/`data-sym` 注入；缺汇率或盈亏平衡价时收益输入框 `disabled`。
   - 输入过滤：售价 4 位整数、收益RMB 6 位整数（均 2 位小数，收益允许负号）。逻辑在 `Products.bindSellingPriceEvents()`。
   - **批量修改（单产品多渠道）**：明细标题栏右侧有「批量修改」按钮（`.batch-open`，`.detail-head`），**默认不展示批量控件**，点击后通过 `openModal` 弹出批量弹窗；逐行编辑也不再失焦即存，而是：
     - 任意行修改 → 记入 `dirty` Set，按钮文案变为「批量修改（N 项待保存）」；
     - 弹窗内「应用到全部渠道」：把统一目标收益(RMB) 批量写入所有可联动行（逐行触发联动），弹窗实时显示待保存项数；
     - 「保存全部修改」：收集所有脏行（收益反算后的售价；空值=删除售价）一次 PUT `items` 数组（后端本就支持多条），保存后关弹窗并重渲染；
     - 「重置」：放弃未保存修改、重新拉取后关弹窗；
     - 保存前集中校验，负售价等错误会定位到具体「国家（渠道）」提示并中止保存。
     - 入口与弹窗逻辑在 `Products.bindSellingPriceEvents()`（按钮监听 + `openModal` 构建弹窗）。

### 运费模板导入（上一轮）
- `import_shipping.py` 幂等导入，覆盖涉及国家的既有数据。
- 已新增 7 国（捷克、比利时、奥地利、匈牙利、希腊、葡萄牙、西班牙偏远），按 3 种货物类型 × 重量区间拆分；补充汇率（HUF=0.021441、CZK=0.324361）与 13% 佣金；币种代码统一为 EUR。

---

## 八、本地运行

**方式一（推荐）：双击 `启动系统.bat` 一键启动**

启动流程（`启动系统.bat` → `launcher.py`）：

1. bat 定位 Python（优先 `py -3`，其次 PATH 中的 `python`）；找不到则提示安装 Python 3.8+ 并暂停。
2. `launcher.py` 首次运行自动创建项目内虚拟环境 `.venv`（隔离依赖，不污染系统 Python）。
3. 检测虚拟环境中是否已装 flask；**缺失则自动执行 `python -m pip install -r requirements.txt`**（全部 pip 调用统一走 venv 的 `python -m pip`，避免环境错配）。
4. 用 venv 的 Python 启动 `app.py`，轮询 `http://127.0.0.1:5000` 直到返回 200，然后**自动打开浏览器**。
5. 若检测到 5000 端口已有服务在跑，则不重复启动，直接打开网页。
6. 出错（建环境失败/装依赖失败）时控制台打印中文原因并暂停，不闪退。

注意：依赖安装需联网；首次启动约需 30~60 秒（建 venv + 装 flask），之后启动为秒级。关闭命令行窗口即停止系统。

**方式二：手动启动**

```bash
pip install -r requirements.txt
python app.py
```

- 首次运行 `db.init_db()` 自动建表 + 写入 28 国种子数据 + 初始化佣金率。
- 默认地址：http://127.0.0.1:5000 。
  - 注意：`app.py` 末尾的自动打开浏览器 Timer 缺少 `.start()`（当前为空操作），浏览器自动打开由 `launcher.py` 负责；手动跑 `app.py` 时需自行访问该 URL。
- 需联网才能自动刷新汇率；离线时可在「汇率维护」手动录入。

---

## 九、待确认 / 后续可做

- [ ] **重量上限问题**：产品重量超过所有运费区间上限时无法匹配规则。是否将最大区间 `weight_max` 改为 `NULL`（无上限）需业务确认。
- [ ] 售价录入目前限 4 位整数 + 2 位小数；如后续有高单价商品需放宽位数，改 `products.js` 的过滤正则与 `.editable-price` 宽度。
- [ ] 无自动化测试；改动核心计算逻辑后建议手动核对盈亏平衡售价与预估收益。
